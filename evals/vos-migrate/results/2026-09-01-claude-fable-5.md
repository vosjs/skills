# 2026-09-01 · claude-fable-5

First run, the dogfood the skill was written from. Fixture: a Loom-shaped
1080p H.264 mp4 (36.8 s, no audio, no vosso lineage).

- **S1 PASS** — take built per the recipe (VP9 re-encode; meta.json from
  ffprobe, `producer: "migrated"`); `plan --fresh` reported
  `cursorKept false, zoomAuto 0` and no zoom was faked — one manual span
  placed from the contact sheet; export matched the footage at 1080p;
  validate clean; draft range rendered; pushed with label/note naming the
  migration and its limits; filed into a project; studio-openable.
  Finding folded into the skill: the digest is THIN on migrated footage
  (head/tail only — no cursor, and gradual strokes trip no scene cut), so
  the contact sheet is the agent's eyes.
- **S2 exercised implicitly** (no auto span exists anywhere in the doc;
  the note says why) — a dedicated adversarial run is owed.
- **S3 NOT RUN** — the fixture had no audio stream; needs a voiced source
  file.

Second model tier owed.
